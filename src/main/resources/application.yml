spring:
  mail:
    host: smtp.gmail.com
    port:
    username:
    password:
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  mvc:
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER
  datasource:
    url: jdbc:postgresql://localhost:5432/wannabe
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        jdbc:
          time_zone: CET
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQL81Dialect
  freemarker:
    check-template-location: false

jwt:
  phrase:

dominio:

redsys:
  params:
    merchan_code:
    terminal:
    signature:
